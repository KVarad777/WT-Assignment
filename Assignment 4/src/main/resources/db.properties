# =========================================================
# MySQL Database Configuration for Electricity Bill System
# Standard XAMPP Configuration (Host: localhost, Port: 3306)
# =========================================================
db.driver=com.mysql.cj.jdbc.Driver
db.host=localhost
db.port=3306
db.name=electricity_bill_db
db.user=root
db.password=
db.useSSL=false
db.allowPublicKeyRetrieval=true
db.serverTimezone=UTC
